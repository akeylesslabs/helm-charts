# Render-only fixture for the SSH bastion securityContext assertions (helm template).
# Not named "*-values.yaml" so `ct install` skips it, the bastion cannot come up in kind.
fullnameOverride: sra-ci
dockerRepositoryCreds: eyJhdXRocyI6e319Cg==
apiGatewayURL: https://rest.akeyless.io

privilegedAccess:
  accessID: p-dummy
  accessKey: dummy-key

ztbConfig:
  enabled: false
  service:
    type: ClusterIP
ztpConfig:
  enabled: false
  service:
    type: ClusterIP

sshConfig:
  enabled: true
  service:
    type: ClusterIP
  config:
    CAPublicKey: "ssh-ed25519 AAAAdummy"
